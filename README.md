# FamiBook（GitHub Pages 門面）

私人圖書館的公開門面。書本體在家裡 `F:\book`，不上這個庫。

線上入口：<https://weslie4436.github.io/famibook/>

家裡保險庫（`python -m mybook.vault`，埠 8766）經 Cloudflare 隧道連上來。隧道網址寫進 `config.js` 的 `VAULT_ORIGIN`。鑰匙不進 git。
